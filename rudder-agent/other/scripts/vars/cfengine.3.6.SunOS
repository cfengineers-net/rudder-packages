OBJECTS=(
	"prepare"
	"zlib"
	"liblmdb"
#	"libiconv"
#	"libxml2"
#	"pcre"
#	"tokyocabinet"
#	"openssl" 
#	"cfengine"
)

SCRIPTDIR=$PWD
BUILDDIR="$PWD/../build"

VARDIR=/var/cfengine.test
PREFIX=/var/cfengine.test
CFE_WORKDIR=$VARDIR

#export AR=/opt/csw/bin/gar

CP=gcp
MAKE=gmake
PATCH=gpatch

NAME="CFEngine Community"
PKGNAME="CFNcfengine-community"
VERSION="3.6.0"
DESCRIPTION="CFEngine community (C) CFEngine AS 2008-. Packaged by www.cfengineers.net <contact@cfengineers.net>"

SPOOL=$PREFIX.spool
SPOOL_SRC=$SPOOL/src/cfengine/$VERSION
CFE_NAMING=`echo $OS | tr '[A-Z]' '[a-z]'`
